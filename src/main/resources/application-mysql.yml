spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/fund_copilot?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&rewriteBatchedStatements=true
    username: root
    password: 123456
  sql:
    init:
      mode: never

fund-copilot:
  observation:
    admin-endpoints-enabled: ${FUND_OBSERVATION_ADMIN_ENABLED:false}
  market-data:
    eastmoney:
      demo-fallback-enabled: ${FUND_MARKET_DEMO_FALLBACK_ENABLED:false}
